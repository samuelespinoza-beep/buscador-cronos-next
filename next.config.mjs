/** @type {import('next').NextConfig} */
// Sin rewrites: las llamadas al backend pasan por los route handlers de src/app/api,
// que son los que agregan las credenciales del lado del servidor.
const nextConfig = {};

export default nextConfig;
